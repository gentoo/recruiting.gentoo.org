.panel.panel-info
  .panel-heading
    .panel-title Forgot your password?
  .panel-body
    = simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, class: 'form-inline' }, wrapper: :inline_form) do |f|
      = f.error_notification
      = f.input :email, :required => true, placeholder: "Enter email"
      = f.button :submit, "Send me reset password instructions", class: 'btn-primary'
  .panel-footer= render "devise/shared/links"
